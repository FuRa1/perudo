{
  "name": "perudo-game",
  "version": "0.0.0",
  "private": true,
  "description": "Networked Perudo (Liar's Dice / Dudo) for 2-12 players, \"Pirates of the Caribbean\" theme.",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run build -w shared && concurrently -n server,client -c blue,green \"npm run start:dev -w server\" \"npm run start -w client\"",
    "dev:android": "adb reverse tcp:4200 tcp:4200 && adb reverse tcp:3000 tcp:3000 && npm run dev",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "test": "npm run test -w shared && npm run test -w server && npm run test -w client",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w client",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "e2e": "npm run e2e -w client"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "angular-eslint": "21.4.0",
    "concurrently": "^10.0.4",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "globals": "^17.9.0",
    "prettier": "^3.9.6",
    "typescript": "~5.9.2",
    "typescript-eslint": "^8.66.0"
  }
}
